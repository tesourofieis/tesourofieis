---
title: Missa do dia
---

import { getDay } from "../../../../lib/getDay.ts";
import { yyyyMMDD } from "../../../../lib/src/utils.ts";

export const day = getDay(yyyyMMDD(new Date()));

<div>
  {day.sections
    .filter((_, idx) => idx > 2)
    .map((section) => (
      <>
        <h2 id={section.id.toLowerCase()}>{section.id}</h2>
        <div className="grid grid-cols-2 gap-5">
          <span class="text-justify">{section.body.latin}</span>
          <span class="text-justify">{section.body.vernacular}</span>
        </div>
      </>
    ))}
</div>
